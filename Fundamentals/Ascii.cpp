#include <iostream>
using namespace std;
int main()
{
    //Ascii value converter
    char letter;
    cout<<"Enter a One Letter: ";
    cin>>letter;
    cout<<"Corresponding ASCII value for "<<letter<<" is "<<int(letter);
    return 0;
}